Descriptions:

+ color
   Color files and symbols
    + color::paint
      · Coloring and styling a piece of text
    + color::change_ascii
      · Changing symbols to ASCII 256


+ display_game
  · For displaying formatted sections of the game in the console
    + format_text()
      · Adjusting string length and filling empty spaces with a specified character
    + start_display()
      · Function for displaying fixed parts of the match table, called at the beginning of each match
    + round_display()
      · Function for displaying variable parts of the match table, called at the beginning of each round
    + group_display()
      · For displaying the status of wizards in each group during a team match


+ logic
  · Game logic implementation
    + round_fight()
      · Logic for each round of a match
    + fight()
      · Logic for each match
    + group_fight()
      · Logic for executing a group game


+ selection
  · Creating a selection list with navigation controls
    + soloSelection()
      · Selecting a single item from a list
    + multiSelection
      · Selecting multiple items from a list